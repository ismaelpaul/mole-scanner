import { ThemedText } from '@/components/ThemedText';
import { ScrollView } from 'react-native';

export default function ScanScreen() {
	return (
		<ScrollView>
			<ThemedText type="title">Welcome to the Scan Screen!</ThemedText>
		</ScrollView>
	);
}
